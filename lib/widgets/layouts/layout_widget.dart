import 'package:flutter/material.dart';

class LayoutWidget extends StatelessWidget {
  final Widget body;

  const LayoutWidget({super.key, required this.body});

  @override
  Widget build(BuildContext context) => body;
}
